import 'package:flutter/material.dart';

class MoodVideo extends StatelessWidget {
  const MoodVideo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
